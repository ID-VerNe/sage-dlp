## Direction 2: 「数据胶囊」— 极简纯科技

一个简化的浏览器窗口轮廓向右延伸出一条弧线，末端接入一个圆点，象征数据包从浏览器流向桌面端。

**概念：** 微窗口 + 数据弧 = 浏览器扩展的核心功能。极度简洁，16px 分辨率为第一优先级。

**视觉：** 仅用 2-3 个几何块，硬边矢量，无渐变纯色填充。

---

Final GPT Image 2 Prompt:

Create an original, non-infringing logo for "SageDLP", a browser extension that sends cookies from the browser to a desktop application.

Brand personality:
Modern, minimal, reliable, developer-friendly. Precision and simplicity first.

Visual direction:
A minimal browser window outline (simplified square with a tiny rounded top bar) on the left, connected by a smooth short arc (like a flowing data packet) to a small filled circle on the right — representing data flowing from browser tab to desktop app. The entire shape fits within a square bounding box.

Style:
Extremely minimal geometric vector. Two or three solid shapes. Hard edges, no gradients. Favicon-first design — must be recognizable at 16x16 pixels.

Color:
Solid deep blue (#2B5B84) on a transparent background. One color only.

Output:
A single icon centered on a transparent-feel background. No text, no letters, no complex shapes. Prioritize readability at 16x16.

Constraints:
Original design only. No trademarks. No watermark. No text. No gradients. No mockup scene. No fine lines thinner than 2px effective at 16px scale.

Suggested settings:
size: 1024x1024
quality: high